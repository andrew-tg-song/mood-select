{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "lerna run lint",
    "format": "prettier -w .",
    "start": "lerna run start",
    "build": "lerna run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2",
    "prettier": "^2.8.8"
  }
}
